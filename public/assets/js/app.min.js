window.App={getCsrfToken(){const meta=document.querySelector('meta[name="csrf-token"]');return meta?meta.getAttribute('content'):null},async refreshCsrfToken(){try{const response=await fetch('/csrf-refresh',{method:'GET',headers:{'X-Requested-With':'XMLHttpRequest'}});if(response.ok){const data=await response.json();const meta=document.querySelector('meta[name="csrf-token"]');if(meta&&data.token){meta.setAttribute('content',data.token);return data.token}}}catch(error){console.warn('Failed to refresh CSRF token:',error)}return null},toast(type,msg){const el=document.createElement('div');el.className=`toast align-items-center text-bg-${type} position-fixed bottom-0 end-0 m-3`;el.role='alert';el.innerHTML=`<div class="d-flex"><div class="toast-body">${msg}</div><button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast"></button></div>`;document.body.appendChild(el);new bootstrap.Toast(el,{delay:3500}).show()},async fetchJson(url,opts={}){const method=(opts.method||'GET').toUpperCase();const needsCsrf=['POST','PUT','PATCH','DELETE'].includes(method);if(needsCsrf){const token=this.getCsrfToken();if(token){opts.headers={'X-CSRF-TOKEN':token,...opts.headers};if(opts.body instanceof FormData&&!opts.body.has('_token')){opts.body.append('_token',token)}if(opts.body&&typeof opts.body==='string'&&opts.headers['Content-Type']?.includes('application/json')){try{const jsonBody=JSON.parse(opts.body);jsonBody._token=token;opts.body=JSON.stringify(jsonBody)}catch(e){}}}}const res=await fetch(url,{headers:{'X-Requested-With':'XMLHttpRequest',...opts.headers},...opts});if(res.status===419&&needsCsrf){const newToken=await this.refreshCsrfToken();if(newToken){opts.headers['X-CSRF-TOKEN']=newToken;if(opts.body instanceof FormData){opts.body.delete('_token');opts.body.append('_token',newToken)}if(opts.body&&typeof opts.body==='string'&&opts.headers['Content-Type']?.includes('application/json')){try{const jsonBody=JSON.parse(opts.body);jsonBody._token=newToken;opts.body=JSON.stringify(jsonBody)}catch(e){}}const retryRes=await fetch(url,{headers:{'X-Requested-With':'XMLHttpRequest',...opts.headers},...opts});const ct=retryRes.headers.get('content-type')||'';if(!ct.includes('application/json'))throw new Error('Invalid response type');const data=await retryRes.json();if(!retryRes.ok||data?.ok===false)throw new Error(data?.error||'Request failed');return data}}const ct=res.headers.get('content-type')||'';if(!ct.includes('application/json'))throw new Error('Invalid response type');const data=await res.json();if(!res.ok||data?.ok===false)throw new Error(data?.error||'Request failed');return data},performance:{marks:{},timings:{},mark(name){this.marks[name]=performance.now()},measure(name,startMark,endMark=null){const start=this.marks[startMark];const end=endMark?this.marks[endMark]:performance.now();this.timings[name]=end-start;return this.timings[name]},getTimings(){return {...this.timings}},clearTimings(){this.marks={};this.timings={}}},loadScript(src,callback=null,options={}){if(document.querySelector(`script[src="${src}"]`)){if(callback)callback();return}const script=document.createElement('script');script.src=src;script.async=options.async!==false;script.defer=options.defer||false;if(options.crossorigin)script.crossOrigin=options.crossorigin;if(callback){script.onload=callback;script.onerror=()=>console.error(`Failed to load script: ${src}`)}document.head.appendChild(script)},preloadResource(href,as,type=null){if(document.querySelector(`link[href="${href}"]`))return;const link=document.createElement('link');link.rel='preload';link.href=href;link.as=as;if(type)link.type=type;link.crossOrigin='anonymous';document.head.appendChild(link)},optimizeImages(){if(!window.IntersectionObserver)return;const imageObserver=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){const img=entry.target;if(img.dataset.src){img.src=img.dataset.src;img.classList.remove('lazy-image');img.classList.add('lazy-loaded');imageObserver.unobserve(img)}}})},{rootMargin:'50px 0px',threshold:0.01});document.querySelectorAll('.lazy-image').forEach(img=>{imageObserver.observe(img)})},init(){this.performance.mark('app_init_start');document.addEventListener('DOMContentLoaded',()=>{this.performance.mark('dom_ready');this.optimizeImages();this.performance.mark('app_init_end');this.performance.measure('app_init_time','app_init_start','app_init_end');this.performance.measure('dom_ready_time','app_init_start','dom_ready')});if('serviceWorker'in navigator){navigator.serviceWorker.register('/sw.js').catch(()=>{})}}};App.init();